---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/evaluation/__init__.py
  - src/evaluation/regex_baseline.py
  - src/evaluation/metrics.py
  - src/evaluation/evaluator.py
  - scripts/evaluate.py
  - tests/test_regex_baseline.py
  - tests/test_metrics.py
  - tests/test_docs.py
  - README.md
  - .env.example
autonomous: true
requirements: [EVAL-02, EVAL-03, DOCS-01, DOCS-02]

must_haves:
  truths:
    - "RegexBaseline.extract(text) returns character-offset spans for all 10 German reference types"
    - "compute_entity_metrics() returns correct entity-level P/R/F1 via seqeval for known BIO input"
    - "scripts/evaluate.py runs and prints P/R/F1 for the regex baseline on sample sentences"
    - "README.md exists with project description, setup guide, usage examples, and Mermaid pipeline diagram"
    - ".env.example exists with OPENROUTER_API_KEY placeholder"
  artifacts:
    - path: "src/evaluation/regex_baseline.py"
      provides: "RegexBaseline class with extract() method"
      exports: ["RegexBaseline"]
    - path: "src/evaluation/metrics.py"
      provides: "spans_to_bio() and compute_entity_metrics() functions"
      exports: ["spans_to_bio", "compute_entity_metrics"]
    - path: "src/evaluation/evaluator.py"
      provides: "Evaluator that runs baseline and computes metrics"
      exports: ["Evaluator"]
    - path: "scripts/evaluate.py"
      provides: "CLI entry point for evaluation"
    - path: "README.md"
      provides: "Project documentation with Mermaid diagram"
      contains: "mermaid"
    - path: ".env.example"
      provides: "Env var template"
      contains: "OPENROUTER_API_KEY"
  key_links:
    - from: "src/evaluation/evaluator.py"
      to: "src/evaluation/regex_baseline.py"
      via: "import RegexBaseline"
      pattern: "from.*regex_baseline.*import.*RegexBaseline"
    - from: "src/evaluation/evaluator.py"
      to: "src/evaluation/metrics.py"
      via: "import spans_to_bio, compute_entity_metrics"
      pattern: "from.*metrics.*import"
    - from: "scripts/evaluate.py"
      to: "src/utils/config.py"
      via: "import load_config"
      pattern: "from.*config.*import.*load_config"
---

<objective>
Build the regex baseline for German legal references, the seqeval metrics wrapper, the evaluation entry point, and project documentation.

Purpose: Establishes the benchmark that the ML model must beat. Without a measured regex baseline, there is no success criterion for the PoC. Also completes all Phase 1 documentation requirements.
Output: Working regex baseline producing P/R/F1 numbers on sample text, README with Mermaid diagram, .env.example.
</objective>

<execution_context>
@/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01-01 — config and device utilities -->

From src/utils/config.py:
```python
from omegaconf import DictConfig

def load_config(config_path: str = "config/default.yaml", overrides: list[str] | None = None) -> DictConfig:
    """Load YAML config and apply CLI overrides."""
```

From src/utils/device.py:
```python
import torch

def get_device() -> torch.device:
    """Auto-detect best available device: CUDA > MPS > CPU."""

def set_seed(seed: int) -> None:
    """Set seeds for PyTorch, NumPy, and Python random."""
```

From config/default.yaml (relevant keys):
```yaml
project:
  seed: 42
evaluation:
  output_dir: "evaluation_output"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regex baseline, metrics wrapper, and tests (RED then GREEN)</name>
  <files>
    src/evaluation/__init__.py, src/evaluation/regex_baseline.py, src/evaluation/metrics.py,
    tests/test_regex_baseline.py, tests/test_metrics.py, tests/conftest.py
  </files>
  <behavior>
    - test_paragraph_ref: extract("Gemäß § 25a KWG gilt") returns span covering "§ 25a KWG"
    - test_paragraph_with_absatz: extract("§ 25a Abs. 1 KWG") returns span covering full reference
    - test_artikel: extract("Art. 6 DSGVO regelt") returns span for "Art. 6 DSGVO"
    - test_anhang: extract("Anhang II CRR enthält") returns span for "Anhang II CRR"
    - test_verordnung: extract("EU-Verordnung 648/2012") returns span for the full reference
    - test_multi_paragraph: extract("§§ 3, 4 UWG") returns at least one span
    - test_satz: extract("§ 5 Abs. 2 S. 1 BGB") returns span including Satz
    - test_nr: extract("§ 1 Nr. 3 KWG") returns span including Nr
    - test_lit: extract("§ 2 Abs. 1 lit. a BGB") returns span including lit
    - test_tz: extract("Tz. 4 MaRisk") returns span for Teilziffer reference
    - test_no_reference: extract("Dies ist ein normaler Satz.") returns empty list
    - test_entity_metrics_perfect: compute_entity_metrics with identical y_true/y_pred returns P=R=F1=1.0
    - test_entity_metrics_partial: compute_entity_metrics with one missed entity returns recall=0.5
    - test_spans_to_bio_basic: spans_to_bio on known text/spans produces correct B-REF/I-REF/O sequence
  </behavior>
  <action>
    Create src/evaluation/__init__.py (empty).

    Update tests/conftest.py to add fixtures with sample German regulatory sentences paired with their expected reference spans (character offsets). Include all 10 reference types from research. Include a negative example (no references).

    Create tests/test_regex_baseline.py with RED tests for all reference types. Each test calls RegexBaseline().extract(text) and asserts the returned spans cover the expected reference text.

    Create tests/test_metrics.py with RED tests for compute_entity_metrics() and spans_to_bio().

    Run pytest to confirm RED.

    Then implement:

    src/evaluation/regex_baseline.py — RegexBaseline class:
    - Uses `regex` PyPI package (NOT stdlib `re`) per research recommendation
    - Compiled GERMAN_LEGAL_REF_PATTERN from research Pattern 4 (covers all 10 types: §, §§, Art., Abs., Nr., lit., Satz, Tz., Anhang, Verordnung)
    - extract(text: str) -> list[tuple[int, int]] returning (start, end) character offset spans
    - Use regex.finditer() to get all non-overlapping match spans

    src/evaluation/metrics.py:
    - spans_to_bio(text: str, char_spans: list[tuple[int, int]]) -> tuple[list[str], list[str]]:
      Whitespace-tokenize text, compute each token's char range, map to B-REF/I-REF/O based on overlap with spans. Return (tokens, bio_labels).
    - compute_entity_metrics(y_true: list[list[str]], y_pred: list[list[str]]) -> dict:
      Wraps seqeval classification_report, precision_score, recall_score, f1_score. Returns dict with keys: precision, recall, f1, report.

    Run pytest to confirm GREEN.
  </action>
  <verify>
    <automated>pytest tests/test_regex_baseline.py tests/test_metrics.py -x -v</automated>
  </verify>
  <done>RegexBaseline.extract() correctly identifies all 10 reference types. spans_to_bio produces correct BIO labels. compute_entity_metrics returns correct P/R/F1. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Evaluator, evaluate script, README, .env.example, and doc tests</name>
  <files>
    src/evaluation/evaluator.py, scripts/evaluate.py,
    README.md, .env.example, tests/test_docs.py, data/gold_test/.gitkeep, data/cache/.gitkeep
  </files>
  <action>
    Create data/gold_test/.gitkeep and data/cache/.gitkeep if they don't exist (directory structure).

    Create src/evaluation/evaluator.py — Evaluator class:
    - __init__(self, cfg: DictConfig): Store config
    - evaluate_baseline(self, samples: list[dict]) -> dict: Takes list of {"text": str, "spans": list[tuple[int,int]]} dicts. For each sample, runs RegexBaseline.extract() to get predicted spans. Converts both gold and predicted spans to BIO via spans_to_bio(). Calls compute_entity_metrics() on the batch. Returns metrics dict.
    - format_report(self, metrics: dict) -> str: Pretty-prints P/R/F1 table.
    - Samples parameter uses gold spans as ground truth, regex spans as predictions.

    Create scripts/evaluate.py entry point:
    - Calls load_dotenv() first
    - Loads config via load_config()
    - Detects device via get_device() and logs it
    - Sets seed via set_seed()
    - Creates hardcoded sample sentences with known reference spans (at least 5 diverse samples covering major reference types) as demo data. In Phase 4 this will read from gold_test_set.json.
    - Runs Evaluator.evaluate_baseline() on samples
    - Prints the formatted report

    Create .env.example with single line: OPENROUTER_API_KEY=your_api_key_here

    Create README.md with:
    - Project title and one-paragraph description
    - Mermaid pipeline diagram showing: Config -> Data Gen -> BIO Convert -> Train -> Evaluate (with regex baseline branch)
    - Setup section: pip install, .env setup, config overview
    - Usage section: scripts/evaluate.py example with sample output
    - Project structure tree
    - Requirements section mentioning Python 3.10+

    Create tests/test_docs.py:
    - test_readme_exists: Assert README.md exists and contains "```mermaid"
    - test_env_example_exists: Assert .env.example exists and contains "OPENROUTER_API_KEY"

    Run full test suite.
  </action>
  <verify>
    <automated>pytest tests/ -x -v</automated>
  </verify>
  <done>scripts/evaluate.py runs and prints P/R/F1 for regex baseline on sample sentences. README.md has Mermaid diagram. .env.example has API key placeholder. All tests pass including doc tests.</done>
</task>

</tasks>

<verification>
pytest tests/ -v — all tests green (config + regex + metrics + docs)
python scripts/evaluate.py — prints regex baseline P/R/F1 report
cat README.md | grep -c mermaid — returns at least 1
cat .env.example — contains OPENROUTER_API_KEY
</verification>

<success_criteria>
- RegexBaseline correctly extracts all 10 German legal reference types
- seqeval metrics wrapper produces entity-level P/R/F1 (not token-level)
- scripts/evaluate.py runs end-to-end and prints a formatted evaluation report
- README.md contains project description, setup guide, usage, and Mermaid diagram
- .env.example contains OPENROUTER_API_KEY placeholder
- All tests in test_regex_baseline.py, test_metrics.py, and test_docs.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
